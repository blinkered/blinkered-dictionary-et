# What this evidence rests on: et

Generated by `pnpm collections` from the committed evidence.

Every collection below was read once and its testimony written into `attestations/`, along with
the number of playable tokens it held. That total is the denominator of every rate, and recording
it is what makes the downloaded text **disposable**: a later build that adds a family reuses these
lines instead of re-reading gigabytes to rediscover them.

So the dumps are not kept. This table is the pointer back, and a rebuild that wants to re-read a
collection rather than reuse its record only has to put the file back where the path says.

| collection | tokens | what it is | where it came from |
| --- | ---: | --- | --- |
| `wiki:et` | 56,872,163 | Estonian Wikipedia; modern encyclopedic prose | [etwiki.xml.bz2](https://dumps.wikimedia.org/etwiki/latest/etwiki-latest-pages-articles.xml.bz2) |
| `lz:est_newscrawl_2017_1M` | 1,685,587 | Leipzig est_newscrawl_2017_1M; modern news, cited by the page each sentence came from | [est_newscrawl_2017_1M](https://downloads.wortschatz-leipzig.de/corpora/est_newscrawl_2017_1M.tar.gz) |
| `ia` | 943,424 | Internet Archive Estonian books; literature, and the register a newspaper never reaches | [archive-et](https://archive.org/search?query=language%3Aest%20AND%20mediatype%3Atexts) |
| `wikisource:et` | 463,507 | Estonian Wikisource; same Wikimedia family, so it corroborates rather than counts | [etwikisource.xml.bz2](https://dumps.wikimedia.org/etwikisource/latest/etwikisource-latest-pages-articles.xml.bz2) |
| `lz:est_news_2022_300K` | 362,775 | Leipzig est_news_2022_300K; modern news, cited by the page each sentence came from | [est_news_2022_300K](https://downloads.wortschatz-leipzig.de/corpora/est_news_2022_300K.tar.gz) |
| `web:looming.ee` | 318,891 |  | fetched from `looming.ee`, recorded in `searched.tsv` |
| `web:sirp.ee` | 273,572 |  | fetched from `sirp.ee`, recorded in `searched.tsv` |
| `web:aripaev.ee` | 250,616 |  | fetched from `aripaev.ee`, recorded in `searched.tsv` |
| `web:err.ee` | 219,169 |  | fetched from `err.ee`, recorded in `searched.tsv` |
| `web:lounaeestlane.ee` | 179,274 |  | fetched from `lounaeestlane.ee`, recorded in `searched.tsv` |
| `ebible:ekk` | 114,996 | Pühakiri kaasaegses eesti keeles, a modern Estonian New Testament; a family nothing else here belongs to | [ekk_vpl.txt](https://ebible.org/Scriptures/ekk_vpl.zip) |
| `web:postimees.ee` | 100,105 |  | fetched from `postimees.ee`, recorded in `searched.tsv` |
| `web:delfi.ee` | 56,942 |  | fetched from `delfi.ee`, recorded in `searched.tsv` |
| `web:ohtuleht.ee` | 34,001 |  | fetched from `ohtuleht.ee`, recorded in `searched.tsv` |
| `tat` | 24,005 | Tatoeba Estonian; contemporary and conversational | [est_sentences.tsv](https://downloads.tatoeba.org/exports/per_language/est/est_sentences.tsv.bz2) |
| `gut` | 10,447 | Project Gutenberg Estonian | [gutenberg-et](https://www.gutenberg.org/cache/epub/feeds/pg_catalog.csv) |
| `web:eestinaine.ee` | 5,351 |  | fetched from `eestinaine.ee`, recorded in `searched.tsv` |
| `web:pealinn.ee` | 52 |  | fetched from `pealinn.ee`, recorded in `searched.tsv` |

Totals are playable tokens after folding, not bytes. A collection missing from this table was
never scanned; one present with no link was fetched by hand and its path is given instead.
